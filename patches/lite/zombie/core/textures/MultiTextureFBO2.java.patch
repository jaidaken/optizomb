--- /home/jaidaken/pz-optizomb/vanilla/zombie/core/textures/MultiTextureFBO2.java	2026-03-05 19:03:48.751793075 +0000
+++ /home/jaidaken/pz-optizomb/src/zombie/core/textures/MultiTextureFBO2.java	2026-03-07 16:34:57.681666109 +0000
@@ -162,24 +162,35 @@
 
         float float3 = 0.004F * GameTime.instance.getMultiplier() / GameTime.instance.getTrueMultiplier() * (Core.TileScale == 2 ? 1.5F : 1.5F);
         if (!this.bAutoZoom[int0]) {
-            float3 *= 5.0F;
+            float3 *= zombie.optizomb.OptiZombConfig.ZOOM ? zombie.optizomb.OptiZombZoom.MANUAL_ZOOM_MULTIPLIER : 5.0F;
         } else if (this.targetZoom[int0] > this.zoom[int0]) {
             float3 *= 1.0F;
         }
 
         if (this.targetZoom[int0] > this.zoom[int0]) {
-            this.zoom[int0] = this.zoom[int0] + float3;
-            IsoPlayer.players[int0].dirtyRecalcGridStackTime = 2.0F;
-            if (this.zoom[int0] > this.targetZoom[int0] || Math.abs(this.zoom[int0] - this.targetZoom[int0]) < 0.001F) {
-                this.zoom[int0] = this.targetZoom[int0];
+            if (zombie.optizomb.OptiZombConfig.ZOOM) {
+                boolean _reached = zombie.optizomb.OptiZombZoom.stepZoomUp(this.zoom, this.targetZoom, int0, float3);
+                // Always recalc grid stack during zoom, but use lower urgency for intermediate frames
+                IsoPlayer.players[int0].dirtyRecalcGridStackTime = _reached ? 2.0F : 0.5F;
+            } else {
+                this.zoom[int0] = this.zoom[int0] + float3;
+                IsoPlayer.players[int0].dirtyRecalcGridStackTime = 2.0F;
+                if (this.zoom[int0] > this.targetZoom[int0] || Math.abs(this.zoom[int0] - this.targetZoom[int0]) < 0.001F) {
+                    this.zoom[int0] = this.targetZoom[int0];
+                }
             }
         }
 
         if (this.targetZoom[int0] < this.zoom[int0]) {
-            this.zoom[int0] = this.zoom[int0] - float3;
-            IsoPlayer.players[int0].dirtyRecalcGridStackTime = 2.0F;
-            if (this.zoom[int0] < this.targetZoom[int0] || Math.abs(this.zoom[int0] - this.targetZoom[int0]) < 0.001F) {
-                this.zoom[int0] = this.targetZoom[int0];
+            if (zombie.optizomb.OptiZombConfig.ZOOM) {
+                boolean _reached = zombie.optizomb.OptiZombZoom.stepZoomDown(this.zoom, this.targetZoom, int0, float3);
+                IsoPlayer.players[int0].dirtyRecalcGridStackTime = _reached ? 2.0F : 0.5F;
+            } else {
+                this.zoom[int0] = this.zoom[int0] - float3;
+                IsoPlayer.players[int0].dirtyRecalcGridStackTime = 2.0F;
+                if (this.zoom[int0] < this.targetZoom[int0] || Math.abs(this.zoom[int0] - this.targetZoom[int0]) < 0.001F) {
+                    this.zoom[int0] = this.targetZoom[int0];
+                }
             }
         }
 
